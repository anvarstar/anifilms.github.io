/* Сброс отступов/паддингов */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Контейнер с центрированием и отступами */
.container {
  width: 90%;
  max-width: 1200px;
  margin: 0 auto;
}

/* Header */
.site-header {
  background: #111;
  padding: 15px 0;
}
.site-header .logo {
  color: #ff4c4c;
  font-size: 1.8em;
  text-decoration: none;
}
.search-form {
  display: inline-block;
  float: right;
}
.search-form input {
  padding: 8px;
  border: none;
  border-radius: 4px 0 0 4px;
  width: 200px;
}
.search-form button {
  padding: 8px 12px;
  border: none;
  background: #ff4c4c;
  color: #fff;
  border-radius: 0 4px 4px 0;
  cursor: pointer;
}

/* Categories nav */
.categories {
  background: #222;
  padding: 10px 0;
}
.categories ul {
  list-style: none;
  display: flex;
  flex-wrap: wrap;
  gap: 15px;
}
.categories a {
  color: #ddd;
  text-decoration: none;
  font-size: 0.9em;
  transition: color 0.2s;
}
.categories a:hover {
  color: #ff4c4c;
}

/* Сетка фильмов */
.movie-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
  gap: 20px;
  margin: 30px 0;
}
.movie-card {
  background: #111;
  border-radius: 6px;
  overflow: hidden;
  transition: transform 0.2s;
}
.movie-card:hover {
  transform: translateY(-5px);
}
.movie-card img {
  width: 100%;
  display: block;
}
.movie-info {
  padding: 10px;
  text-align: center;
}
.movie-info h3 {
  font-size: 1em;
  color: #fff;
  margin-bottom: 5px;
}
.movie-info span {
  font-size: 0.85em;
  color: #aaa;
}

/* Кнопка «Загрузить ещё» */
.load-more {
  text-align: center;
  margin-bottom: 50px;
}
.load-more button {
  padding: 12px 30px;
  background: #ff4c4c;
  color: #fff;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}

/* Footer */
.site-footer {
  background: #111;
  color: #777;
  padding: 20px 0;
  font-size: 0.85em;
  text-align: center;
}
